import { StyleSheet } from 'react-native';

export const TAB_FONT_SIZE = 18;
export const TAB_ICON_SIZE = 16;
export const ICON_SPACE = 28;

export const sharedStyles = StyleSheet.create({
  pillLayout: {
    alignItems: 'center',
    flexDirection: 'row',
    gap: 4,
    paddingHorizontal: 8,
    paddingVertical: 4,
  },
});
